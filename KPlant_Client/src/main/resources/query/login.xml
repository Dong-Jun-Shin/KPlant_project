<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kplant.client.login.dao.LoginDAO">
	<select id="loginCheck" parameterType="member" resultType="member">
		SELECT * FROM member
		WHERE m_id = #{m_id} AND m_pwd = #{m_pwd}
	</select>
	
	<select id="viewMember" resultType="member">
		SELECT
			m_name as m_name,
			m_id as m_id,
			m_email as m_email,
			m_date as m_date
		FROM member
		WHERE m_id = #{m_id} AND m_pwd =#{m_pwd}
	</select>
	
	<!-- 아이디 찾기 -->
	<select id="lookupId" parameterType="member" resultType="member">
		/*join = lookupId*/
		select m_name,m_birth, m_eMail from member WHERE m_num=#{m_num}
	</select>
</mapper>